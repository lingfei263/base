<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.ffb.base.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppXTheme">
        <activity android:name=".MainActivity" android:theme="@style/NoTranslucentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SampleActivity1"/>
        <activity android:name=".SampleActivity2"/>
        <activity android:name=".SampleActivity3" />
        <activity android:name=".SampleActivity4" />
        <activity android:name=".SampleActivity5" />
    </application>

</manifest>